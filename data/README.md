The dataset used in this project comes from the MedDataSets R package
(heart_transplant_tbl_df).

Raw data files are not included because the dataset is accessed
programmatically within the R script.

